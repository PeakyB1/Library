from django.db import models
from engine.models import Book
# Create your models here.
# class NetBook(models.Model):
#     book = models.ForeignKey(Book, on_delete=models.CASCADE, verbose_name="ID_книги")
#     fb2file = models.FileField(upload_to='books/', max_length=100, null=True, verbose_name="Файл книги")
